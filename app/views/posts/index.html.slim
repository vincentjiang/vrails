- if current_user && current_user.admin?
  .pull-right
    = link_to "新建文章", new_post_path, class: "btn btn-primary btn-sm"

- @posts.group_by{ |posts| posts.created_at.year }.each do |group|
  .row
    .col-md-12
      h4.post-year
        = icon 'archive'
        | &nbsp;
        = group[0]
  ul.list-unstyled.posts-list
    - group[1].each do |post|
      li
        span.post-date
          = post.created_at.strftime("%F")
        = link_to post.title, post, class: "post-title"
